Add the files in "main extension folder" to a new folder called "arrayfind base" wherever you want to keep the data for the addon
add the "icons" to a folder named "icons" and add it to the "arrayfind base"
create individual folders for the databases
to install, choose open expanded on Chrome and select the "arrayfind base" folder
